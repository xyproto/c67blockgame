println("Testing lambda with loop")

grid := [10]

init_grid = {
    @ i in 0..<10 max 10 {
        grid[i] <- i * 2
    }
}

init_grid()

println("Grid initialized")

@ i in 0..<10 max 10 {
    printf("grid[%v] = %v\n", i, grid[i])
}

println("Done")
