println("Testing lambda with loop but no array")

counter := 0

count_fn = {
    @ i in 0..<5 max 5 {
        counter <- counter + 1
    }
}

count_fn()

printf("Counter after loop: %v\n", counter)
println("Done")
